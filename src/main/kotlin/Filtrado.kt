class Filtrado {

        fun sinPares(array: Array<Int>){
            println(array.filter{it%2!=0}) //numeros que su division entre 2 no de resto cero para relaizar filtrado de pares
        }
    }
